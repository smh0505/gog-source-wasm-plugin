[package]
name = "gog-source-wasm-plugin"
version = "0.2.0"
edition = "2024"

[dependencies]
wit-bindgen-rt = { version = "0.44.0", features = ["bitflags"] }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "gamelib:plugin"
target = { world = "source-plugin-world", path = "wit/plugin.wit" }

[package.metadata.component.dependencies]
